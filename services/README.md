# services/

One subfolder per homelab service. Each holds notes, ports, config paths, and runbooks for that service.

Deployment compose files live on the server at `/home/dgmneto/homelab/services/<svc>/compose.yaml`, not here. This is the knowledge layer.
