# Access — Sonarr

- **URL:** https://sonarr.intern.dgmneto.com (nginxIntern → `sonarr:8989`)
- **Credential:** `op://Homelab/Sonarr` (user `dgmneto`). API key also in
  `/footage/services/sonarr/config/config.xml`.
- **Auth:** app login form. User completes login.

Reach via the Claude-in-Chrome extension (see root `../../CLAUDE.md`).
