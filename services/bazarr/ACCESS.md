# Access — Bazarr

- **URL:** https://bazarr.intern.dgmneto.com (nginxIntern → `bazarr:6767`)
- **Credential:** **not in 1Password yet.** App login + API key live in `/footage/services/bazarr/config`.
- **Auth:** app login form (or none, depending on config). User completes login.
- **TODO:** add a `Bazarr` login item to the `Homelab` vault.

Reach via the Claude-in-Chrome extension (see root `../../CLAUDE.md`).
