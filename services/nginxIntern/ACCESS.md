# Access — nginxIntern (NPM admin)

- **URL:** https://nginx.intern.dgmneto.com (→ `nginxIntern:81`, the NPM admin UI)
- **Credential:** `op://Homelab/nginx` (user `dgmneto@gmail.com`).
- **Auth:** Nginx Proxy Manager admin login.
- **Note:** this instance owns all `*.intern.dgmneto.com` proxy hosts; their mappings live in the NPM
  SQLite DB at `/footage/services/nginxIntern/data/database.sqlite`.

Reach via the Claude-in-Chrome extension (see root `../../CLAUDE.md`).
