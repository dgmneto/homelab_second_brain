# Access — nginxProd (NPM admin)

- **URL:** https://nginxprod.intern.dgmneto.com (→ `nginxProd:81`, the NPM admin UI)
- **Credential:** `op://Homelab/NGINX Prod` (user `dgmneto@gmail.com`).
- **Auth:** Nginx Proxy Manager admin login.
- **Note:** this is the **public-facing** proxy; it owns the `*.3e.dgmneto.com` hosts (e.g. Plex
  `filmin.3e.dgmneto.com`). Mappings in `/footage/services/nginxProd/data/database.sqlite`.

Reach via the Claude-in-Chrome extension (see root `../../CLAUDE.md`).
