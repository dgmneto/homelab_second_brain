# Access — Zigbee2MQTT

- **URL:** https://z2m.intern.dgmneto.com (nginxIntern → `192.168.14.96:8080`)
- **Credential:** **not in 1Password yet.** Frontend auth is optional and may be disabled.
- **Auth:** Z2M web frontend.
- **TODO:** add a `Zigbee2MQTT` item to the `Homelab` vault if frontend auth is enabled.

Reach via the Claude-in-Chrome extension (see root `../../CLAUDE.md`).
