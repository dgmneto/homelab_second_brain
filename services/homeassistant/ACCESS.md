# Access — Home Assistant

- **URL:** https://ha.intern.dgmneto.com (nginxIntern → `homeassistant:8123`)
- **Credential:** **not in 1Password yet.** HA local user accounts (created at onboarding).
- **Auth:** HA login form. User completes login.
- **TODO:** add a `Home Assistant` login item to the `Homelab` vault.

Reach via the Claude-in-Chrome extension (see root `../../CLAUDE.md`).
