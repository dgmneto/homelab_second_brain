# Access — Radarr

- **URL:** https://radarr.intern.dgmneto.com (nginxIntern → `radarr:7878`)
- **Credential:** `op://Homelab/Radarr` (user `dgmneto`). API key also in
  `/footage/services/radarr/config/config.xml`.
- **Auth:** app login form. User completes login.

Reach via the Claude-in-Chrome extension (see root `../../CLAUDE.md`).
